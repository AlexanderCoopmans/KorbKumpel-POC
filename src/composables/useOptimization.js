// This composable has been removed. The optimization logic was stripped from
// the app per request — only the OptimizeButton trigger remains. Delete this
// file manually if it is no longer needed.
export {}
