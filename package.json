{
  "name": "korbkumpel",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "format": "prettier --write --experimental-cli src/",
    "deploy": "npm run build && git push origin --delete gh-pages && git add -f dist && git commit -m \"Temporary Build for gh-pages\" && git subtree push --prefix dist origin gh-pages && git reset HEAD~1"
  },
  "dependencies": {
    "@vueuse/core": "^14.3.0",
    "pinia": "^3.0.4",
    "vue": "^3.5.38",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@unocss/transformer-directives": "^66.7.4",
    "@vitejs/plugin-vue": "^6.0.7",
    "prettier": "3.8.4",
    "unocss": "^66.7.4",
    "unocss-preset-daisy": "^7.0.0",
    "vite": "^8.0.16",
    "vite-plugin-vue-devtools": "^8.1.2"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  }
}
